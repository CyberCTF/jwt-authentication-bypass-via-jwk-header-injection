name: Build and Push Docker image

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      
      - name: Install dependencies
        run: |
          cd test
          pip install -r requirements.txt
      
      - name: Start application
        run: |
          docker compose -f deploy/docker-compose.dev.yml up -d --build
          # Wait for application to be healthy
          timeout 60 bash -c 'until docker compose -f deploy/docker-compose.dev.yml ps | grep -q "healthy"; do sleep 2; done'
      
      - name: Run tests
        run: |
          cd test
          python -m pytest . -v
      
      - name: Stop application
        if: always()
        run: |
          docker compose -f deploy/docker-compose.dev.yml down

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_ORGA }}
          password: ${{ secrets.DOCKER_OAT }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./build/Dockerfile
          push: true
          tags: cyberctf/jwt-authentication-bypass-via-jwk-header-injection:latest
      - name: Update DockerHub README
        uses: peter-evans/dockerhub-description@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PAT }}
          repository: cyberctf/jwt-authentication-bypass-via-jwk-header-injection
          readme-filepath: ./deploy/README.md
